<article class="article {{post_class}}">
    <img class="thumbnail" src="{{feature_image}}"/>
    <div class="content">
        <span class="category">{{primary_tag.name}}</span>
        <span class="title">{{title}}</span>
        <span class="text-preview">
            {{excerpt words="50"}}...
        </span>
    </div>
    <a href="{{url}}" class="button dark">Lesen</a>
</article>
